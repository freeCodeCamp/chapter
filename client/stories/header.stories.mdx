import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import Header from 'client/ui/header';

<Meta title="Header" component={Header} />

# Header or AppBar

The `Header` component will be used across the whole app, as a Layout.

<Preview>
  <Story name="basic">
    <Header />
  </Story>
</Preview>
