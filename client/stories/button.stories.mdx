import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, text } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";

import Button from 'client/ui/Button';
import GoogleButton from 'client/ui/Button/google';
import { GoogleIcon } from 'client/icons';
import { GridContainer } from "./custom-decorators/components";
import { centered } from './custom-decorators';

<Meta title="Chapter/Button" component={Button} decorators={[withKnobs, centered]} />

# Buttons

We are using Material UI's default buttons, without any customizability.

<Preview className="custom-preview">
  <Story name="basic">
    <GridContainer cols={5}>
      <div>
      <Button onClick={action('Default Clicked')} variant="contained">{text('Default Text', 'Default')}</Button>
      </div>
      <div>
      <Button onClick={action('Primary Clicked')} variant="contained" color="primary">
        Primary
      </Button>
      </div>
      <div>
      <Button onClick={action('Secondary Clicked')} variant="contained" color="secondary">
        Secondary
      </Button>
      </div>
      <div>
      <Button variant="contained" disabled>
        Disabled
      </Button>
      </div>
      <div>
      <Button variant="contained" color="primary" href={'/?path=/story/chapter-navbar--basic'}>
        Link to Navbar
      </Button>
      </div>
    </GridContainer>
  </Story>
</Preview>


## Stories


__Login with Google__

This is a Custom button, as we would be supporting [Google's Brand Guidelines](https://developers.google.com/identity/branding-guidelines)

<Preview className="custom-preview">
    <Story name="with icons">
      <GoogleButton onClick={action('Login Clicked Clicked')} >
        Login with Google
      </GoogleButton>
  </Story>
</Preview>
