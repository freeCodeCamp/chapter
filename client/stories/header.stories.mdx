import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import Navbar from 'client/ui/Navbar';

<Meta title="Navbar" component={Navbar} />

# Navbar or AppBar

The `Navbar` component will be used across the whole app, as a Layout.

<Preview>
  <Story name="basic">
    <Navbar />
  </Story>
</Preview>
