import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, optionsKnob as options } from "@storybook/addon-knobs";
import { withMockRouter } from "./custom-decorators";

import Navbar from 'client/ui/Navbar';
import { links } from "./header-links";

<Meta title="Chapter/Navbar" component={Navbar} decorators={[withKnobs, withMockRouter()]} />

# Navbar or AppBar

The `Navbar` component will be used across the whole app, as a Layout.
Following `Navbar` displays 4 Menu Links, where each `link` structure should match
the following:

```ts
interface INavLinks {
  name: string;
  label: string;
  href: string;
}
```

<Preview className="custom-preview">
  <Story name="basic">
    <Navbar links={links} />
  </Story>
</Preview>
